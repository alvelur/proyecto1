version: "2"
services:
  nginx:
    container_name: nginx-server1.0
    image: nginx
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    ports:
      - "80:80"
    depends_on:
      - nodeapp
  nodeapp:
    container_name: nodeapp1.0
    restart: always
    build: .
    ports:
      - "3000:3000"
    depends_on:
      - mongo-server      
  mongo-server:
    container_name: mongo-server1.0
    image: mongo
    volumes:
      - ./data:/data/db
    ports:
      - "27017:27017"